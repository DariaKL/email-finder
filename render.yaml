services:
  - type: web
    name: email-finder
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: "10000"
      - key: AIRTABLE_BASE
        sync: false
      - key: AIRTABLE_TOKEN
        sync: false
